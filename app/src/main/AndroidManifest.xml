<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.playerapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppPlayer"
        android:allowBackup="true"
        android:icon="@drawable/ic_music_player_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_music_player_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.song.SongActivity" />
        <activity android:name=".ui.player.PlayerActivity"/>
    </application>

</manifest>